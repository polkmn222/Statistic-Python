질문:: Null이나 결측치가 존재한다
       그러면 우리가 단순히 대치를 하게 되는데
       어떠한 숫자로 대체를 하게 되며,
       
       대표적으로 어느 상황에서 그것을 
       적용하는가?? 

fillna 그러면 어떠한 값으로 대체를 하느냐??

1) 최대값 대체
2) 최소값 대체 
3) 최빈값/ 평균값 대체
4) 컬럼값에서 40%이상의 값이 결측치 -> 해당 컬럼삭제 